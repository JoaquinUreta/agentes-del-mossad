import http.client
import BarraBusqueda

class ClienteHTTP():
    def __init__(self):
        pass
    """# Conecta el puerto 80
    conn = http.client.HTTPConnection("www.utalca.cl", 80)
    # Solicita mediante GET
    conn.request("GET", "/")
    # Se guarda codigo de respuesta
    response = conn.getresponse()

    # Se muestra el codigo
    print(response.status, response.reason)
    # Se muestra el contenido de la pagina web
    print(response.read().decode()) 
    conn.close()"""
    def buscarurl(url):
        busqueda = http.client.HTTPConnection(url,80) #usa puerto 80
        busqueda.request("GET", "/")
        